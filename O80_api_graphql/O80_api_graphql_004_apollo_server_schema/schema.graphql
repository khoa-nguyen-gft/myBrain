type Query {
    test: String
    greeting: String
    students: [Student]
    StudentById(id: ID!):Student
}

type Student{
    id: ID!,
    firstName:String
    lastName:String
    password:String
    collegeId:String
}
